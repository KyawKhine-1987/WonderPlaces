package com.android.freelance.famousplaces.ui.viewmodel

import android.app.Application
import android.content.Intent
import android.widget.ImageView
import android.widget.TextView
import androidx.lifecycle.AndroidViewModel
import androidx.lifecycle.MutableLiveData
import com.android.freelance.famousplaces.R

class WondersDetailViewModel(application: Application, intent: Intent) : AndroidViewModel(application) {

    /*private val LOG_TAG = WondersDetailViewModel::class.java.name

    @Bindable
    val detailImage = intent.getStringExtra("image")
    val detailLocation = intent.getStringExtra("title")
    val detailDescription = intent.getStringExtra("desp")*/
}